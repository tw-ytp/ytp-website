{
  "name": "ytp-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/gen_sitemap.mjs",
    "preview": "astro preview",
    "test:mask": "node --experimental-strip-types --no-warnings --test src/lib/mask.test.ts"
  },
  "dependencies": {
    "astro": "^5.0.0"
  }
}
